las siglas cvtxy2xz
significan:
cvt: converter
x: simple o packed
y: tamaño original (s(float), d(double), i(int))
2: to (convierte tal cosa A (to) tal otra)
z: tamaño destino (s(float), d(double), i(int))

cuando aplicamos con truncado se le agrega una t y la podemos considerar de truncado y seguramente viene de esa palabra pero en ingles que creo que es truncate/truncated truncar y truncado resp.